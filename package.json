{
  "name": "Easy-Pixi-Engine",
  "description": "A PixiJS-based game engine inspired by the UI/UX of professional tools like Unity and Unreal Engine.",
  "author": "Javideas",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "clean": "rm -rf dist && rm -rf dist-js",
    "dev": "vite",
    "dev:electron": "electron .",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:electron": "electron-builder",
    "transpile:electron": "tsc --project src/electron/tsconfig.json",
    "restart": "npm run transpile:electron && npm run build && electron .",
    "start": "electron .",
    "dist:mac": "npm run transpile:electron && npm run build && electron-builder --mac --arm64",
    "dist:win": "npm run transpile:electron && npm run build && electron-builder --win --x64",
    "dist:linux": "npm run transpile:electron && npm run build && electron-builder --linux --x64"
  },
  "devDependencies": {
    "electron": "^33.0.1",
    "electron-builder": "^24.13.3",
    "rollup-plugin-copy": "^3.5.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "pixi.js": "^7.4.2"
  },
  "build": {
    "appId": "com.javideas.easy-pixi-engine",
    "productName": "EasyPixiEngine",
    "icon": "./Arcade_Logo.png",
    "files": [
      "dist",
      "dist-js"
    ],
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage",
      "category": "Utility"
    },
    "win": {
      "target": [
        "portable",
        "msi"
      ]
    }
  }
}
